// Pure client-side SPA: no server-side rendering, no prerendering. This is what
// lets the same build run in the browser and inside a Capacitor mobile shell.
export const ssr = false;
export const prerender = false;
