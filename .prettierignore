node_modules
client/build
client/.svelte-kit
server/data
data
package-lock.json

# Generated by drizzle-kit. Reformatting it would make `db:generate` produce a
# diff on every run and fail the CI check that they match.
server/drizzle
